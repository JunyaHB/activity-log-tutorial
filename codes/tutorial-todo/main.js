// Vue.jsの記述
new Vue({
  el: "",
  data: {
  },
  methods: {
    addName: function() {
      // ref で名前を付けておいた要素を参照
      // フォーム要素を空にする
    },
    addActivity: function() {
      // ref で名前を付けておいた要素を参照
      // 配列に加える
      // フォーム要素を空にする
    }
  },
});

